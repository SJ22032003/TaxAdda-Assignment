const initialState = {
  gstin_tags: ["Sample", "URL's"],
  gstin_data: [],
};

export const MainReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
